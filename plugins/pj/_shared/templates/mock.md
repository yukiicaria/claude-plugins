---
name: <mock-id>
feature: <feature>
updated: YYYY-MM-DD
---

# モック: <画面名>

> **忠実に再現する。** DS が設定されていれば語彙とトークンをそれに当てる（visual.md §V2）。
> 既定は全部再現。「どこがこだわりか」を実装者が選り分けない。

## 出典（世代を積む — 古い行は消さない）

実体は [`index.html`](index.html)（埋め込みアセットは剥がしてある）。差し替えるたびに**上に1行足す**。
なぜ作り直したかが後から辿れることが目的（visual.md §V2「差し替え」）。

| 世代 | 取り込み日 | 原典 | 差し替えの理由 |
|---|---|---|---|
| 1 | YYYY-MM-DD | <生成元の URL / 作成手段> | 初版 |

## 対象

- **feature**: [[<feature>]]
- **画面**: <どの画面か>

## 構成（再現義務・ID は振らない）

見比べれば検出できるものは ID を持たない。`pj:design-reviewer` がモック実体と実装を突き合わせる。

- **レイアウト**: <ペイン構成・固定/可変・主要な寸法>
- **配置**: <主要操作をどこに置くか。ツールバーの左右など>
- **列**: <列の順序と役割>
- **密度・書式**: <行高・数値の揃え方・桁区切りなど>

## 挙動（UX-NN — 静的に見比べても検出できないもの）

> ここだけ ID を振る。実装は `@satisfies <mock-id>-UX-NN` を注釈し、grep で鎖を機械検証する
> （ID 規約は concepts §1.1）。1画面あたり数本に収まるのが健全。

- **(<mock-id>-UX-01) <挙動名>** — <入力と結果。閾値があれば数値で>。→ `index.html:<line>`

## DS との衝突（あれば）

DS を当てるとモックの意図が壊れた箇所と、その結果足した DL。**黙って折衷しない**（visual.md §V2）。

| 箇所 | モックの意図 | DS の既定 | 決定 |
|---|---|---|---|
| <箇所> | <意図> | <DS の挙動> | DL-NN として記録 / モックに合わせる / DS に合わせる |

## spec に上げたもの

モックを作る過程で気づいた**振る舞い（WHAT）**は spec が正。ここには反映先だけ残す。

- <気づき> → [[<feature>]] AC-NN

## UX 採番台帳

次番: <mock-id>-UX-01。廃止 UX は**欠番**にし再利用しない（concepts §1.1）。
**モックを作り直しても ID は振り直さない**（残る挙動は同じ ID を維持・消えた挙動は欠番・増えた挙動は新番）。

- **欠番**: <ID> — <なぜ廃止したか。どの世代の差し替えで消えたか>

## 変更履歴

- YYYY-MM-DD: <変更内容>
