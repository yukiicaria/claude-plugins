# <package 名>

> **これは package product**（concepts §2）。app を知らない独立ライブラリとして作る。
> 用語は [glossary.md](glossary.md) が正。

## これは何か

（1〜2行。**別の app でもそのまま通じる言い方**で書けること。その package の責務の語彙は使ってよい。
特定 app の制度に固有の語でしか説明できないなら、それは package ではなく feature）

## 解く問題

（何の面倒を見るライブラリか。利用側が自分で書かずに済むようになるものは何か）

## 公開する API（表面）

> **利用側から見た形**を先に決める。実装の詳細は `docs/design/` へ。
> 「使うのが簡単か」がライブラリの品質。呼び出しが数行で済む形を目指す。

```
<主要な入口の形。関数名・型名・戻り値の概形>
```

## 知らないこと（境界）

> **この package が知らないものを明示的に書く。** ここが曖昧だと app の語彙が滲み込む。

- <この package の責務の外にある概念。特に app 固有の制度・他 feature の語彙>
- <永続層 / 外部サービス / FW など、アダプタとして注入されるもの>

## 依存

> **`package → app` は禁止**（concepts §2）。依存は一方向のみ・循環しない。

| 依存先 | 何のために |
|---|---|
| <@scope/other> | <理由> |

## 準拠する作法

> 作法は root の `docs/design/conventions.md` が正（concepts §13）。ここには**従っている項目名だけ**書く。
> 内容を写すとそこがドリフトする。

- <作法名>

## 機能

> ここは「**何があるか**」だけを持つ。**完了状態は持たない**（進捗の正は下のダッシュボード1箇所）。
> feature が少ないうちはこの overview に受入条件ごと畳んでよい（concepts §6）。

- <feature-name> — <一行説明>

## 受入条件

> **spec が1ファイルのうちはここに直接書く**（concepts §6）。AC ID は段階1から必ず振る。
> 育って `features/` に割るときも **ID は変えない**（concepts §4）。

- [ ] (<feature>-AC-01) <検証可能な条件>

## 非機能要件

（性能・互換性・パッケージサイズなど）

## 進捗ダッシュボード（2軸: spec / build）

| 機能 | spec | build | 残課題 |
|---|---|---|---|
| <name> | □□□□□ | □□□□□ | 未着手 |

**全体 spec 進捗**: □□□□□ (平均 0/5)
**全体 build 進捗**: □□□□□ (平均 0/5)

> 更新したら root の product 表（上段）も引き直す（concepts §9）。

## 未確定の論点

- <論点>

## 変更履歴

- YYYY-MM-DD: `/pj:setup package` で器を作成。中身は未定義。
