---
name: <intake-id>
fidelity: sketch | structure | hifi
imported: YYYY-MM-DD
updated: YYYY-MM-DD
---

# 取り込み: <成果物の名前>

> **出典であって正ではない。** 正は下の振り分け表で `AC-NN` / `DL-NN` になったものだけ（visual.md §V2）。

## 忠実度

**`<sketch | structure | hifi>`**。<なぜその宣言か。出典側の申告があればそれを引く>

| fidelity | 実装が拘束されるもの |
| --- | --- |
| `sketch` | 構造と並び順だけ。**数値は拾わない** |
| `structure` | 構造 ＋ 挙動。見た目は DS に委ねる |
| `hifi` | 上記 ＋ 明記された寸法・閾値・時間 |

## 出典（世代を積む — 古い行は消さない）

実体はこのディレクトリに置いてある（同梱アセットは剥がしてある）。差し替えるたびに**上に1行足す**。
なぜ作り直したかが後から辿れることが目的（visual.md §V2「差し替え」）。

| 世代 | 取り込み日 | 原典 | 差し替えの理由 |
| --- | --- | --- | --- |
| 1 | YYYY-MM-DD | <Artifact の URL / 受け取った経路> | 初版 |

## 正 / 出典 / 参考

| 立場 | ファイル | 扱い |
| --- | --- | --- |
| **出典** | `<実体>` | 見比べる基準。直接は拘束しない |
| **参考** | `<reference/*.tsx / types/*.ts など>` | **意図を読むためだけ。写経しない** |

**参考実装をコピーしない。** ルーティング・状態管理・命名は実装先の作法に合わせる。
出典のフレームワーク前提を持ち込むと、その repo の作法が壊れる。

## 振り分け（この出典から起こした ID）

**判定は「テストできるか」1つ。** できるなら AC、できず横断なら DL、どちらでもないなら振らない。

| 出典の記述 | 判定 | 行き先 |
| --- | --- | --- |
| <例: compact の行高は 39px> | テストできる | `(<feature>-AC-NN)` |
| <例: 影は使わない・角丸 4px> | 横断の原則 | `DL-NN` |
| <例: 余白と列順> | 見比べれば分かる | 振らない（実体と突き合わせ） |

> **AC の採番者はここではない。** 新規は `/pj:spec`、増減は `/pj:change`（concepts §4）。
> intake は**どこに落とすかを決めるところまで**。

## 拾わなかったもの（と理由）

書かないと、次に読む人が「見落とし」か「意図的に外した」かを区別できない。

- <例: fidelity が `sketch` なので数値は拾わなかった>
- <例: 出典が React Router 前提。実装先は Next.js なので作法に置き換えた>
- <例: 出典に「v2」と明記されているので今回は対象外>

## 未描画 ≠ 否定

出典に**描かれていないこと**は「否定」ではなく「未描画」が既定。
エラー状態・空状態・権限による非表示・バリデーションは描かれないのが普通。
既存 AC を廃止すべきだと判断したら、**必ず確認してから `/pj:change`（L2）で spec から直す**。

## 変更履歴

- YYYY-MM-DD: <変更内容>
