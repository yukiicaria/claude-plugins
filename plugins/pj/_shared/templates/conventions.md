---
progress: 0
updated: YYYY-MM-DD
---

# コード規約 / 横断方針（作法）

> spec が「実装裁量」と投げた**横断項目を1回ここで決め、全 feature で統一**する。
> 各実装にゆだねるとバラつく事故を防ぐのが目的。
>
> **このファイルは root product が所有し、repo の全 product が準拠する**（concepts §13）。
> package も従うが、**従うことは依存ではない**。package を切り出すときは、baked-in の設計判断として
> その package の `docs/design/` へ写して連れて行く。

## product 構成（repo にライブラリを持つ場合）

> **「全 package 共通の◯◯」と書きたくなったものは、app の spec ではなく必ずここに来る**（concepts §13）。
> 該当が無ければこの節ごと削ってよい。

- **package を割る基準** … <この repo で採用する基準（product-model.md §P1）。**層の名前で先に決めない**>
- **外界の触り方** … <永続・外部送信・ドメイン解決をどう注入させるか>
- **時間の扱い** … <期間・時点・タイムゾーンの統一規約>
- **スキーマとマイグレーション** … <誰が DDL を打つか・テーブル名の接頭辞>
- **トランザクション** … <誰が握り、利用側の処理をどう差し込ませるか>

## フォルダ構成 / 命名

### 配置の表（**必須**・concepts §12）

> **build はこの表を引くだけで、新しいトップレベルの箱を作らない。** 表に無いものが要ると気づいたら、
> その場で作らず `/pj:change` に投げる（表を増やすこと自体が作法の変更）。

| 分類 | 何を置くか | 置き場 |
|---|---|---|
| **挙動** | ルール・状態・不変条件（見た目なしでテストが書ける） | <例: `src/core/`> |
| **見た目** | トークン・原則に沿った部品 | <例: `src/parts/`> |
| **まとめる** | 配線・注入・ページ組み立て | <例: `src/app/`> |

（その他のファイル/ディレクトリ命名・レイヤー分け）

## 横断方針（プロジェクト固有の確定事項）
- **<横断項目>** … <統一方針>（spec の非機能要件 / 実装裁量項目から拾う）
  - 例: 入力処理の共通ユーティリティ、同時編集の競合制御、リッチエディタ選定、ファイル添付方式 等

## テスト方針
- 受入条件 → test の対応規律（build フェーズの契約）
- テストの置き場所・命名・粒度（unit / 結合 / E2E の使い分け）

## 変更履歴
- YYYY-MM-DD: <変更内容>
