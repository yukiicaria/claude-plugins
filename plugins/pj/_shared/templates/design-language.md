---
progress: 0
updated: YYYY-MM-DD
---

# デザイン言語 — 視覚の正（canonical）

> 視覚の正。全画面・全コンポーネントが従う。用語が glossary.md に従うのと対称。
> 各原則に安定 ID (DL-NN) を振り、コンポーネントは満たす DL を `@satisfies DL-NN` で注釈する
> （ID 規約の正は concepts §4。**DL の追加/廃止は `/pj:design`・`/pj:change` が行う**）。
> UI を持たないプロジェクトでは、本文を消して `# デザイン言語 — N/A（UI を持たないプロジェクト）` とだけ宣言してよい。

## 導出インタビュー（spec は視覚を明記しない → ここを埋めて原則を導く）

- 主な利用者と熟練度（パワーユーザー / たまに使う人）→ 密度・キーボード重視度（DL-01,05,19）
- 1 画面で扱うデータ量・頻度（多数の行を毎日 / 少数をたまに）→ DataTable 標準化の閾値（DL-12）
- デバイス（PC 主 / モバイル併用）→ ハンドル / タッチ・密度
- ブランド / トーン（堅実・先進・親しみ）と既存色資産 → 色の意味論・primary アクセント（DL-08〜11）
- 既存の正（カラーパレット / トークン）→ DL-09 / DL-10 が指す先

## 0. 北極星（一文）

<このプロダクトの視覚方針を一文で>

## A. 階層と強調

- **(DL-NN) <原則>** — <検証可能な内容。RefactoringUI: 階層は size/weight/color、強調は競合の de-emphasis、ボタンのピラミッド>

## B. 情報密度とレイアウト

- **(DL-NN) <原則>** — <region 分割・整列の一貫性・spacing/typography/radius/elevation スケール>

## C. 色の意味論

- **(DL-NN) <原則>** — <色は意味のみ・装飾しない／既存パレットが正／セマンティックトークン bg/foreground ペア／primary は画面に1つ（Linear）>

## D. インタラクション

- **(DL-NN) <原則>** — <一覧は検索/ソート/フィルタ／二値少数フィルタはセグメント・トグル／確定値はインライン編集／集中編集は中央 Dialog／並べ替えは可視ハンドル＋キーボード>

## E. アクセシビリティ

- **(DL-NN) <原則>** — <キーボード完結・フォーカストラップ・aria・コントラスト基準>

## F. コンポーネント語彙とプリミティブ族（共通化の正）

> ベースのデザインシステムがあれば、ここで**語彙・トークンの正を1つに固定**する。
> **ベース語彙・トークンの正 = <DS パッケージ名／preset>**（DS があれば DL-NN はその差分だけ。preset は DL-NN が全部を担う）。
> 各プリミティブ族は**単一窓口**を決め、再発明させない（導線は `src/components/CLAUDE.md`）。

- **(DL-NN) コンポーネント語彙** — canonical な語彙を共通化し再発明しない／プリミティブとテーブル/DnD のライブラリ選定（ADR と接続）
- **(DL-NN) アイコン** — 単一ライブラリのみ（例: `<DS の Icon>` / `lucide-react`）。**生 `<svg>`・絵文字をアイコン代わりにしない・別ライブラリを足さない**
- **(DL-NN) 色** — トークン経由のみ。**生の hex / rgb・Tailwind の任意値（`bg-[#...]`）を書かない**（§C と接続）
- **(DL-NN) その他のプリミティブ族** — 日付/数値フォーマット・トースト・空状態・イラスト等も単一窓口を決める（必要になったら採番）

## DL 採番台帳

次番: DL-01。廃止 DL は欠番にし再利用しない（concepts §4）。欠番はここに理由を1行残す。

## 変更履歴

- YYYY-MM-DD: 初版。
