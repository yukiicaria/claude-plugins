---
name: change
description: pj パイプラインの「直したい」唯一の入口。開発者は層を意識せず自然文で投げるだけ。能動（○○を変えたい）と受動（もう code を直した／ズレてる）を自動判別し、正しいレイヤーで直してトレースの鎖（受入条件↔test↔code / design↔実装）を伝播・整合させる。
disable-model-invocation: true
argument-hint: "[<変えたいこと（自然文）> | reconcile]"
---

pj で作ったプロジェクトを「直したい」ときの**唯一の入口**。開発者は spec/design/build の層を知らなくていい。
自然文で言うだけで、pj が方向と正しいレイヤーを判定し、全部の整合を取る。

引数: $ARGUMENTS

## 最初に必ず読む

共通の軸・モード・保守規律・トレースの鎖は **`../../_shared/concepts.md`**（pj パイプラインの背骨）。
起動したらまず読む。**変更の高度トリアージ（L0 コスメ〜L4 根本）の正は concepts §9**。下記手順はその実行版。
**UI に触れる変更と判定したら `../../_shared/visual.md` も読む**（§V1 視覚の正 / §V2 モック）。
この skill は「正しいレイヤーから直して下へ伝播」（concepts §4・§9）を**開発者に代わって実行する**ルーター。

**編集はこの skill が自分で行う。**他 skill の「軽量修正モード」に委譲しない —— そのモードは
**ID を増減しないもの専用**で、増減するものを `/pj:change` に送り返す側だから（concepts §3 モード5）。
委譲すると送り返し合いになる。編集の作法（変更履歴・`updated`・readiness・影響1個提示）は
**concepts §4 の手順に従う**（作法を借りるのであって、モードを呼ぶのではない）。

## 手順

### 1. 意図と状況の把握
- `$ARGUMENTS`（自然文の変更要望）を読む。
- 文脈と**未反映の変更**を確認する: `git status` / `git diff` で手編集済みの code がないか、
  `docs/` 各成果物の `updated` と実装の新しさにズレがないか（drift 兆候）を見る。
- **運用宣言の管理ブロックを保証**: ルート `CLAUDE.md` に `<!-- pj:managed -->` ブロックが無ければ stamp する
  （concepts.md §10。/pj:change は編集系フローなので、初回着地時にここで焼き込まれる）。

### 2. 方向の自動判別
- **能動（トップダウン）**: 新しい振る舞い・技術・挙動への変更要望 → 正のレイヤーを**先に**直し、下へ伝播。
- **受動（ボトムアップ / reconcile）**: code/docs が**既にズレている**（手で code を直した・引数が `reconcile`・
  drift 検出）→ 実態に合わせて docs を追従させる、または差分を提示して「どちらが正か」を決める。
- 判別がつかなければ**一言だけ確認**する。

### 3. 正のレイヤーを特定して宣言
- 振る舞い・仕様（WHAT）→ `docs/specs/`
- 技術・構造（HOW）→ `docs/design/`
- 純粋なバグ／実装都合（挙動は仕様どおり）→ code / test のみ

特定したら**必ず一言宣言する**: 「これは〈振る舞い/技術/実装〉の変更なので **〈ファイル〉が正**。そこを直して
〈下流〉に伝播します」。これにより、層を知らない開発者が**使うたびに受動的にモデルを学ぶ**（覚える義務はない）。

### 4. 適用＋トレースの鎖の伝播
- **能動**: 正レイヤーを編集 → 受入条件が増減したら **AC を採番**（`(<feature>-AC-NN)` 形式、concepts §1.1）→
  対応する test を追加/更新（落ちる→緑）→ code を実装 → **影響 AC のテスト緑を確認**。
- **受動**: code の実態から対応する AC/spec を辿り、docs を追従させる（不足 AC を採番して追加、test を補完）。
  実態と仕様の**どちらが正か曖昧なら提示して確認**（勝手に上書きしない）。
- **UI に触れる変更**: `docs/design/design-language.md`（視覚の正・visual.md §V1）を参照する。新しいデザイン原則が
  要るなら **DL-NN を自動採番**して design-language に追記し、`DL ↔ コンポーネント(@satisfies DL-NN) ↔ 使用箇所` の
  鎖を伝播する。整合確認は review/audit で **`pj:design-reviewer`** を使える。
- **モックのある画面に触れる変更**（`docs/design/mocks/<mock-id>/` がある・visual.md §V2）:
  - **体験そのものを変えるなら、モックを先に更新してから実装に伝播**する（実装だけ変えるとモックが嘘になる）。
    挙動が増減したら `mock.md` の **UX-NN を採番/欠番化**し、`@satisfies` を追従させる。
  - **DS を当てるとモックの意図が壊れた**という報告を受けたら、折衷せず **DL-NN を1本採番**して
    design-language と `mock.md` の「DS との衝突」表に記録する。
  - モック起因で**振る舞い（WHAT）**が変わるなら、それは L2 以上。spec を正として AC を採番し下流へ伝播する。
  - **「実装を見たが気に入らない。モックを作り直した」**（リモック）は **`/pj:design mock <mock-id> <新モック>`**
    に案内する。差し替えは AC を壊さないための固有の安全確認（テスト緑の確認・AC の1件ずつ照合・
    「描かれていない ≠ 否定」の判定）が要り、その手順は design 側が持つ（visual.md §V2）。
    そこから **AC の廃止が必要**と判定されて戻ってきたら、L2 としてここで spec から直す。
- どちらの方向でも、触れた各レイヤーで **変更履歴・`updated`・overview の2軸ダッシュボードを同期**
  （concepts §4 軽量修正手順 / §5）。前提が覆ったら readiness を**下げる**。

### 5. 報告
- 触れたファイルを**層横断で1リスト**にして提示（spec/design/test/code）。
- テスト結果（緑/赤）。残った未決や readiness 後退があれば明示。
- 大きい変更で関連 feature に波及しそうなら**1個だけ**提示する。

## 採番の実務（規約の正は concepts.md §1.1）

採番者は ID ごとに違う（**正は concepts.md §1.1 の表**）:

- **`(<feature>-AC-NN)` は `/pj:change` が唯一の採番者。**
- **`(DL-NN)` / `(<mock>-UX-NN)` は `/pj:design` と共同**（design の対話・`mock` モードでも採番される）。

いずれも他 skill の**軽量修正モードでは増減しない**。形式・不変ルール・欠番の扱いは §1.1 を見る。
ここでは書き方だけ:

- 受入条件: `- [ ] (<feature>-AC-NN) <検証可能な条件>` → test 名かコメントに同じ ID を含める
  （例: `test('event-AC-12 予算未入力で保存できない', ...)`）
- デザイン原則: `- **(DL-NN) <原則>** — <検証可能な内容>` → コンポーネント先頭に `@satisfies DL-NN`
- モックの挙動: `- **(<mock>-UX-NN) <挙動名>** — <入力と結果>` → 実装に `@satisfies <mock>-UX-NN`
- 採番したら各文書末尾の**採番台帳の次番を進める**。

## やってはいけないこと

concepts.md §8（全レイヤー共通の禁止事項）と §9（高度トリアージの secured 担保）に従う。
`/pj:change` 固有で特に外せないのは1つ:

- **受動時、実態と仕様が食い違うのに確認せずどちらかを上書きする**（どちらが正かはユーザーが決める）。
